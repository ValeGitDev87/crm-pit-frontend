# Prestito in Tasca CRM Frontend — Codex Docs

Copia il contenuto di questo pacchetto nella root del repository `frontend/`.

Struttura attesa:

```text
frontend/
├── AGENTS.md
├── CODEX.md
├── MANIFEST.md
├── docs/
└── src/
```

La reference backend canonica è `docs/99-BACKEND-API-REFERENCE.md`.
